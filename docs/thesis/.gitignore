# ---------------------------------------------------------------------------
# Trình biên dịch tải về (khoảng 49 MB) — build.ps1 tự tải lại khi cần
# ---------------------------------------------------------------------------
tools/

# ---------------------------------------------------------------------------
# Tệp trung gian do LaTeX sinh ra
# ---------------------------------------------------------------------------
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.lof
*.lot
*.run.xml
*.xdv
*.fls
*.fdb_latexmk
*.synctex.gz
main-blx.bib

# ---------------------------------------------------------------------------
# Thư mục trung gian của build-docx.ps1
#
# Chứa bản .tex đã tiền xử lý mà Pandoc thực sự đọc, cùng reference.docx và
# ảnh dựng từ sơ đồ TikZ. Sinh lại được hoàn toàn nên không cần theo dõi.
# ---------------------------------------------------------------------------
_docx/

# ---------------------------------------------------------------------------
# Tệp Word tạm (~$... là tệp khoá Word tạo ra khi đang mở tài liệu)
# ---------------------------------------------------------------------------
~$*.docx

# ---------------------------------------------------------------------------
# Sản phẩm biên dịch
#
# thesis.pdf và thesis.docx ĐƯỢC theo dõi trong git để người đọc lấy được bản
# đọc ngay mà không cần dựng lại. Nếu không muốn vậy, bỏ dấu # ở dòng dưới.
# ---------------------------------------------------------------------------
main.pdf
# thesis.pdf
# thesis.docx
